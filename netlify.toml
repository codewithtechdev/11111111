[build]
  command = "npm run build && npm run export"
  publish = "out"

[build.environment]
  NODE_VERSION = "18"

# Tell Netlify this is a Next.js project, not Angular
[build.processing]
  skip_processing = true
